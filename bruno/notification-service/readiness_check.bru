meta {
  name: Notification Service Readiness Check
  type: http
  seq: 2
}

get {
  url: {{notificationServiceBaseUrl}}/ready
  body: none
  auth: none
}

tests {
  test("should return 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("should have service name", function() {
    expect(res.getBody()).to.have.property('service', 'notification-service');
  });
  
  test("should be ready", function() {
    expect(res.getBody()).to.have.property('status', 'ready');
  });
}
